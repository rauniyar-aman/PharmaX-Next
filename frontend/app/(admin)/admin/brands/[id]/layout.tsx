import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Edit Brand' }

export default function AdminEditBrandLayout({ children }: { children: React.ReactNode }) {
  return children
}
