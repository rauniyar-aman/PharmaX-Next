import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Add Brand' }

export default function AdminAddBrandLayout({ children }: { children: React.ReactNode }) {
  return children
}
