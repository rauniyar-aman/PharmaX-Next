import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Campaigns' }

export default function AdminCampaignsLayout({ children }: { children: React.ReactNode }) {
  return children
}
