import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Pharmacy Details' }

export default function AdminPharmacyDetailsLayout({ children }: { children: React.ReactNode }) {
  return children
}
