import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Add Pharmacy' }

export default function AdminAddPharmacyLayout({ children }: { children: React.ReactNode }) {
  return children
}
