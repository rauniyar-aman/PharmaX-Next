import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Agent Earnings' }

export default function AdminAgentEarningsLayout({ children }: { children: React.ReactNode }) {
  return children
}
