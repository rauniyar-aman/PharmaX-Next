import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Revenue by Channel' }

export default function RevenueChannelLayout({ children }: { children: React.ReactNode }) {
  return children
}
