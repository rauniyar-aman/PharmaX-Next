import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Day-wise Finance' }

export default function AdminFinanceDailyLayout({ children }: { children: React.ReactNode }) {
  return children
}
