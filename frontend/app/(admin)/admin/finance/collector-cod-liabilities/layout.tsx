import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Collector COD Liabilities' }

export default function AdminCollectorCodLiabilitiesLayout({ children }: { children: React.ReactNode }) {
  return children
}
