import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Agent Finance Profile' }

export default function AdminAgentFinanceProfileLayout({ children }: { children: React.ReactNode }) {
  return children
}
