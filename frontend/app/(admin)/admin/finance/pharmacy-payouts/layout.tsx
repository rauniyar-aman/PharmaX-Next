import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Pharmacy Payouts' }

export default function AdminPharmacyPayoutsLayout({ children }: { children: React.ReactNode }) {
  return children
}
