import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'COD Liabilities' }

export default function AdminCodLiabilitiesLayout({ children }: { children: React.ReactNode }) {
  return children
}
