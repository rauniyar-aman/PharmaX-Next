import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Collector Earnings' }

export default function AdminCollectorEarningsLayout({ children }: { children: React.ReactNode }) {
  return children
}
