import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Write Article' }

export default function AdminWriteArticleLayout({ children }: { children: React.ReactNode }) {
  return children
}
