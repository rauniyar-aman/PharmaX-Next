import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Add Admin' }

export default function AdminAddAdminLayout({ children }: { children: React.ReactNode }) {
  return children
}
