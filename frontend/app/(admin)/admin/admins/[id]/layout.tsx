import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Edit Admin' }

export default function AdminEditAdminLayout({ children }: { children: React.ReactNode }) {
  return children
}
