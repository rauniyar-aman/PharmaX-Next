import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Add Lab Collector' }

export default function AdminAddLabCollectorLayout({ children }: { children: React.ReactNode }) {
  return children
}
