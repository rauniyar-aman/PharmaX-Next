import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Prescription Details' }

export default function AdminPrescriptionDetailsLayout({ children }: { children: React.ReactNode }) {
  return children
}
