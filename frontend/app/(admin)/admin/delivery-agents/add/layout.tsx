import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Add Delivery Agent' }

export default function AdminAddDeliveryAgentLayout({ children }: { children: React.ReactNode }) {
  return children
}
