import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Add Doctor' }

export default function AdminAddDoctorLayout({ children }: { children: React.ReactNode }) {
  return children
}
