import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Stock Log' }

export default function AdminStockLogLayout({ children }: { children: React.ReactNode }) {
  return children
}
