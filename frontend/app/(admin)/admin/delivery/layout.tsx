import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Delivery' }

export default function AdminDeliveryLayout({ children }: { children: React.ReactNode }) {
  return children
}
