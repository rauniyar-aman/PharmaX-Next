import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Edit Medicine' }

export default function AdminEditMedicineLayout({ children }: { children: React.ReactNode }) {
  return children
}
