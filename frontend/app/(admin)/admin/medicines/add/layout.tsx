import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Add Medicine' }

export default function AdminAddMedicineLayout({ children }: { children: React.ReactNode }) {
  return children
}
