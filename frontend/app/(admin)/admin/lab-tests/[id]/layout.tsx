import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Edit Lab Test' }

export default function AdminEditLabTestLayout({ children }: { children: React.ReactNode }) {
  return children
}
