import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Add Lab Test' }

export default function AdminAddLabTestLayout({ children }: { children: React.ReactNode }) {
  return children
}
