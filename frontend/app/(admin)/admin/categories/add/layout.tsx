import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Add Category' }

export default function AdminAddCategoryLayout({ children }: { children: React.ReactNode }) {
  return children
}
