import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Edit Category' }

export default function AdminEditCategoryLayout({ children }: { children: React.ReactNode }) {
  return children
}
