import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Lab Test Cart' }

export default function LabCartLayout({ children }: { children: React.ReactNode }) {
  return children
}
