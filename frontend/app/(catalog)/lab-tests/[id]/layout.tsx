import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Lab Test Details' }

export default function LabTestDetailLayout({ children }: { children: React.ReactNode }) {
  return children
}
