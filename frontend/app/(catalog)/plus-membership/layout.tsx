import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Plus Membership' }

export default function PlusMembershipLayout({ children }: { children: React.ReactNode }) {
  return children
}
