import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Appointment Confirmed' }

export default function AppointmentConfirmedLayout({ children }: { children: React.ReactNode }) {
  return children
}
