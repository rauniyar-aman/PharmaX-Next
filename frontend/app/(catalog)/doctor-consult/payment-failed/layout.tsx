import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Payment Failed' }

export default function DoctorConsultPaymentFailedLayout({ children }: { children: React.ReactNode }) {
  return children
}
