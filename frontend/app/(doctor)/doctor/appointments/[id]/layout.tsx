import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Appointment Details' }

export default function DoctorAppointmentDetailLayout({ children }: { children: React.ReactNode }) {
  return children
}
