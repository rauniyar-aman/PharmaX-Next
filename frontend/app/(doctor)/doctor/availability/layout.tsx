import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Availability' }

export default function DoctorAvailabilityLayout({ children }: { children: React.ReactNode }) {
  return children
}
