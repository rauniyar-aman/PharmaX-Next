import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Earnings' }

export default function DoctorEarningsLayout({ children }: { children: React.ReactNode }) {
  return children
}
