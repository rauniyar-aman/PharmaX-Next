import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Restore Account' }

export default function RestoreAccountLayout({ children }: { children: React.ReactNode }) {
  return children
}
