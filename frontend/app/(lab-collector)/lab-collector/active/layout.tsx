import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Active Collections' }

export default function LabCollectorActiveLayout({ children }: { children: React.ReactNode }) {
  return children
}
