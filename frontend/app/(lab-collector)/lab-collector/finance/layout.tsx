import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Lab Collector Finance' }

export default function LabCollectorFinanceLayout({ children }: { children: React.ReactNode }) {
  return children
}
