import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Collection History' }

export default function LabCollectorHistoryLayout({ children }: { children: React.ReactNode }) {
  return children
}
