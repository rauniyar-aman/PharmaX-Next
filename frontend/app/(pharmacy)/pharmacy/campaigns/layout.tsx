import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'My Campaigns' }

export default function PharmacyCampaignsLayout({ children }: { children: React.ReactNode }) {
  return children
}
