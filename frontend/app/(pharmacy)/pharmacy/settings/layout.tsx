import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Pharmacy Settings' }

export default function PharmacySettingsLayout({ children }: { children: React.ReactNode }) {
  return children
}
