import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Pharmacy Inventory' }

export default function PharmacyInventoryLayout({ children }: { children: React.ReactNode }) {
  return children
}
