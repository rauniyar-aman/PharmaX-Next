import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Pharmacy Requests' }

export default function PharmacyRequestsLayout({ children }: { children: React.ReactNode }) {
  return children
}
