import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Pharmacy Finance' }

export default function PharmacyFinanceLayout({ children }: { children: React.ReactNode }) {
  return children
}
