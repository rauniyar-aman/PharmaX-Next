import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Pharmacy Team' }

export default function PharmacyTeamLayout({ children }: { children: React.ReactNode }) {
  return children
}
