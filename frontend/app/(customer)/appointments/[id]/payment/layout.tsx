import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Complete Payment' }

export default function AppointmentPaymentLayout({ children }: { children: React.ReactNode }) {
  return children
}
