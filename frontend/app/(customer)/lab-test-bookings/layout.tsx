import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Lab Test Bookings' }

export default function LabTestBookingsLayout({ children }: { children: React.ReactNode }) {
  return children
}
