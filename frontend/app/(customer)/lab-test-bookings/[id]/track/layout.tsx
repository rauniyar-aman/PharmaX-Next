import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Track Collector' }

export default function TrackCollectorLayout({ children }: { children: React.ReactNode }) {
  return children
}
