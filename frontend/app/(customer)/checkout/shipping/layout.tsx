import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Delivery Address' }

export default function CheckoutShippingLayout({ children }: { children: React.ReactNode }) {
  return children
}
