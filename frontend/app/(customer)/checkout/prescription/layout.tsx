import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Attach Prescription' }

export default function CheckoutPrescriptionLayout({ children }: { children: React.ReactNode }) {
  return children
}
