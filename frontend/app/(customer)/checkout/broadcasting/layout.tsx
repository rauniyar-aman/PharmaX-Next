import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Finding Pharmacy' }

export default function CheckoutBroadcastingLayout({ children }: { children: React.ReactNode }) {
  return children
}
