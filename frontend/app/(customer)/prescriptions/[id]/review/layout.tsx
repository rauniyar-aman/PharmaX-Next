import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Prescription Review' }

export default function PrescriptionReviewLayout({ children }: { children: React.ReactNode }) {
  return children
}
