import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Delivery Finance' }

export default function DeliveryFinanceLayout({ children }: { children: React.ReactNode }) {
  return children
}
