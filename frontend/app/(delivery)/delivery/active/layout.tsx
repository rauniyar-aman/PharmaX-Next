import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Active Deliveries' }

export default function DeliveryActiveLayout({ children }: { children: React.ReactNode }) {
  return children
}
